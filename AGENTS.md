# Gems of Light — Codex / non-Claude agent onboarding

**Read `CLAUDE.md` in this directory first — it is the single onboarding
document for EVERY agent tool, and all of it applies to you verbatim**
(the name is historical; nothing in it is Claude-specific). Do not act
before reading it.

Then, per its read-before-acting list:
1. `v3/BACKLOG.md` — the shared Now/Next/Later list across all agent
   tools. Pick work from NOW.
2. `v3/PLAN.md` — the decision log and source of truth. Verdicts land
   in §10, never in the backlog.

One duplicate copy of the onboarding notes used to live here; it
drifted within a day. Keep this file a pointer.
